{
  "name": "verify",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "sdk",
    "frontend",
    "monitor",
    "server"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build:api": "node scripts/build-api.cjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0"
  }
}
